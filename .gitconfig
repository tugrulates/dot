[user]
	email = tugrulkagan@gmail.com
	name = Tugrul Ates
[filter "lfs"]
	clean = git-lfs clean -- %f
	smudge = git-lfs smudge -- %f
	process = git-lfs filter-process
	required = true
[core]
	editor = code --wait
[credential]
	helper = wincred
